# Required
NGROK_AUTHTOKEN="your-ngrok-authtoken"

# Optional — use a stable ngrok domain (e.g., from ngrok dashboard)
NGROK_DOMAIN=""

# Optional
WEBHOOK_PORT=4040
WEBHOOK_PATH="/webhook"

# Clawdbot notifications (send webhook alerts to user)
CLAWDBOT_NOTIFY_CHANNEL="whatsapp"
CLAWDBOT_NOTIFY_TARGET=""
