# Required
NGROK_AUTHTOKEN="your-ngrok-authtoken"

# Optional — use a stable ngrok domain (e.g., from ngrok dashboard)
NGROK_DOMAIN=""

# Optional
WEBHOOK_PORT=4040
WEBHOOK_PATH="/webhook"

# OpenClaw notifications (send webhook alerts to user)
OPENCLAW_NOTIFY_CHANNEL="whatsapp"
OPENCLAW_NOTIFY_TARGET=""
